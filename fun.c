#include <stdio.h>
#include <string.h>
#include <math.h>
int main (){
// distance function
   int i;
for(i=1; i<3; i++){
    struct memeber Mymember;
    struct memeber memeber[i];
    float latm, lat, distance;
    distance= sqrt((latm+lat)*(latm+lat));
}
}